{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://commos.dev/schemas/events/CallFailed.schema.json",
  "title": "CallFailed",
  "description": "CommOS canonical event 'CallFailed' (Volume 5). Envelope + typed data payload.",
  "allOf": [
    {
      "$ref": "https://commos.dev/schemas/envelope.schema.json"
    }
  ],
  "properties": {
    "type": {
      "const": "CallFailed"
    },
    "data": {
      "type": "object",
      "properties": {
        "call_id": {
          "$ref": "https://commos.dev/schemas/common.schema.json#/$defs/Uuid"
        },
        "cause": {
          "type": "string"
        }
      },
      "required": [
        "call_id",
        "cause"
      ]
    }
  },
  "required": [
    "type",
    "data"
  ]
}
