{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://commos.dev/schemas/events/SecretReferenceUpdated.schema.json",
  "title": "SecretReferenceUpdated",
  "description": "CommOS canonical event 'SecretReferenceUpdated' (Volume 5). Envelope + typed data payload.",
  "allOf": [
    {
      "$ref": "https://commos.dev/schemas/envelope.schema.json"
    }
  ],
  "properties": {
    "type": {
      "const": "SecretReferenceUpdated"
    },
    "data": {
      "type": "object",
      "properties": {
        "secret_ref": {
          "type": "string"
        }
      },
      "required": [
        "secret_ref"
      ]
    }
  },
  "required": [
    "type",
    "data"
  ]
}
