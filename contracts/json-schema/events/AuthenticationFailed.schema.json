{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://commos.dev/schemas/events/AuthenticationFailed.schema.json",
  "title": "AuthenticationFailed",
  "description": "CommOS canonical event 'AuthenticationFailed' (Volume 5). Envelope + typed data payload.",
  "allOf": [
    {
      "$ref": "https://commos.dev/schemas/envelope.schema.json"
    }
  ],
  "properties": {
    "type": {
      "const": "AuthenticationFailed"
    },
    "data": {
      "type": "object",
      "properties": {
        "user_id": {
          "$ref": "https://commos.dev/schemas/common.schema.json#/$defs/Uuid"
        },
        "method": {
          "type": "string"
        },
        "reason": {
          "type": "string"
        }
      },
      "required": [
        "method"
      ]
    }
  },
  "required": [
    "type",
    "data"
  ]
}
